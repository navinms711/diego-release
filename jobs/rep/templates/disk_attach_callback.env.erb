# Disk attach callback config for pre-start. Source this before calling the callback.
# Only used when diego.rep.disk_attach_callback_url is set.
export DISK_ATTACH_CALLBACK_URL="<%= p('diego.rep.disk_attach_callback_url') %>"
export DISK_ATTACH_CALLBACK_AUTH_KEY="<%= p('diego.rep.disk_attach_callback_auth_key') %>"
